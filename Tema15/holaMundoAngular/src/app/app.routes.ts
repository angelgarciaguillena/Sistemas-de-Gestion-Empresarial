import { Routes } from '@angular/router';
import { FormularioReactivo } from '../components/formulario-reactivo/formulario-reactivo';

export const routes: Routes = [
  { path: 'formulario-persona', component: FormularioReactivo }
];
